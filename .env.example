## Public runtime config (Next.js)
# Base URL of the faucet backend API (no trailing slash).
NEXT_PUBLIC_FAUCET_API_BASE_URL=http://localhost:8080

# Cloudflare Turnstile site key (widget key).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# Network display name.
NEXT_PUBLIC_NETWORK_NAME=Catalyst Testnet

# Template used to create tx links. Supported placeholders: <txHash>, {txHash}, :txHash
NEXT_PUBLIC_EXPLORER_TX_URL_TEMPLATE=https://explorer.catalystnet.org/tx/<txHash>

# Comma-separated EVM chainId allowlist (hex or decimal). Default is Catalyst testnet chainId.
NEXT_PUBLIC_ALLOWED_CHAIN_IDS=0xbf8457c

